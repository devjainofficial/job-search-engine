# Server-side only (never exposed to the browser)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
# URL of the FastAPI worker that parses resumes (local dev: http://localhost:8000)
WORKER_URL=http://localhost:8000

# Exposed to the browser (safe): the bot users message to connect Telegram
NEXT_PUBLIC_TELEGRAM_BOT_USERNAME=DevJainBot
